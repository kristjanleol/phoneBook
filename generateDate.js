/*exports.getDate= function(){
        let today = new Date();
        let options = {
        weekday: "long", 
        day: "numeric",
        month: "long"
    }
    let date = today.toLocaleDateString("en-US", options);
    return date;
    }
    exports.getWeekDay = function(){
        let today = new Date();
        let options = {
        weekday: "long"
    }
    
    let dateDay = today.toLocaleDateString("en-US", options);
    return dateDay;
    }*/